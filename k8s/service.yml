apiVersion: v1
kind: Service
metadata:
  name: node-js-devops-project-service
spec:
  selector:
    app: node-js-devops-project
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30008
